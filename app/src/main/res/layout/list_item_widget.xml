<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_list_item_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/bullet_point"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="@dimen/standard_margin" />

    <TextView
        android:id="@+id/widget_list_item_ingredient"
        style="@style/WidgetListItem"
        android:layout_width="0dp"
        android:layout_weight="8"
        android:layout_marginStart="@dimen/small_margin"
        tools:text="Unsalted butter" />

    <TextView
        android:id="@+id/widget_list_item_quantity_with_measure"
        style="@style/WidgetListItem"
        android:layout_width="0dp"
        android:layout_weight="2"
        android:layout_marginBottom="@dimen/small_margin"
        android:layout_marginLeft="@dimen/standard_margin"
        android:layout_marginRight="@dimen/standard_margin"
        android:gravity="end"
        tools:text="tblsp" />

</LinearLayout>